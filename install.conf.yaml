- defaults:
      link:
           relink: true

- clean: ['~']

- link:
#      ~/.dotfiles: ''
      ~/.vim: vim
      ~/.vimrc: vimrc
      ~/.bashrc: bashrc

#- create:
#      - ~/downloads
#      - ~/.vim/undo-history

- shell:
      - [git submodule update --init --recursive, Installing submodules]
